name: wattwatch
version: "0.1.0"
profile: wattwatch

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

target-path: target
clean-targets: [target, dbt_packages]

models:
  wattwatch:
    staging:
      +materialized: view
      +schema: staging
    silver:
      +materialized: table
      +schema: silver
    gold:
      +materialized: table
      +schema: gold

seeds:
  wattwatch:
    +schema: ref
